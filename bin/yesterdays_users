mysql --skip-column-names tourfilter_$1 -u$DB_USERNAME -p$DB_PASSWORD -e'select count(*) from users where (last_visited_on is not null or registration_type="basic") and date_format(registered_on,"%Y/%m/%d")=date_format(adddate(now(),interval -1 day),"%Y/%m/%d")'
